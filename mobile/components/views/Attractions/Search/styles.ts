import { StyleSheet } from 'react-native';

export const attractionSerchStyles = StyleSheet.create({
  attractionsList: {
    backgroundColor: '#fff',
    width: '100%',
    padding: 10,
    marginVertical: 1
  },
  errorText: {
    fontSize: 24,
    color: 'red',
  },
});
